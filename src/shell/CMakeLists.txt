add_library(trafficsim_shell STATIC editor_window.cpp editor_inspector.cpp editor_connectors.cpp editor_files.cpp editor_history.cpp editor_rotation.cpp
    editor_tables.cpp editor_diagnostics.cpp editor_demand.cpp editor_routing.cpp editor_control.cpp editor_run.cpp editor_storage.cpp editor_recovery.cpp editor_palette.cpp editor_creation.cpp
    ../editor/canvas_spatial.cpp ../editor/canvas_rotation.cpp ../editor/canvas.cpp ../editor/canvas_lanes.cpp ../editor/canvas_input.cpp ../editor/canvas_connectors.cpp ../editor/canvas_select.cpp ../editor/canvas_run.cpp ../editor/canvas_display.cpp ../editor/canvas_demand.cpp)
target_link_libraries(trafficsim_shell PUBLIC trafficsim_commands Qt6::Widgets PRIVATE trafficsim_options)
qt_add_executable(trafficsim-desktop main.cpp)
target_link_libraries(trafficsim-desktop PRIVATE trafficsim_shell trafficsim_options)
set_target_properties(trafficsim-desktop PROPERTIES WIN32_EXECUTABLE TRUE)
trafficsim_runtime_data(trafficsim-desktop)
install(TARGETS trafficsim-desktop RUNTIME DESTINATION bin)
add_dependencies(check trafficsim-desktop)
if(BUILD_TESTING)
    find_package(Qt6 6.4 REQUIRED COMPONENTS Test)
    add_executable(trafficsim-rotation-ui-tests ../../tests/rotation_ui_tests.cpp)
    target_link_libraries(trafficsim-rotation-ui-tests PRIVATE trafficsim_shell Qt6::Test trafficsim_options)
    add_test(NAME editor-rotation-ui COMMAND trafficsim-rotation-ui-tests "${PROJECT_SOURCE_DIR}/data")
    set_tests_properties(editor-rotation-ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 60)
    add_dependencies(check trafficsim-rotation-ui-tests)
    add_executable(trafficsim-workflow-ui-tests ../../tests/editor_workflow_ui_tests.cpp)
    target_link_libraries(trafficsim-workflow-ui-tests PRIVATE trafficsim_shell Qt6::Test trafficsim_options)
    add_test(NAME editor-workflow-ui COMMAND trafficsim-workflow-ui-tests "${PROJECT_SOURCE_DIR}/data")
    set_tests_properties(editor-workflow-ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 60)
    add_dependencies(check trafficsim-workflow-ui-tests)
    add_executable(trafficsim-lifecycle-ui-tests ../../tests/network_lifecycle_ui_tests.cpp)
    target_link_libraries(trafficsim-lifecycle-ui-tests PRIVATE trafficsim_shell Qt6::Test trafficsim_options)
    target_compile_definitions(trafficsim-lifecycle-ui-tests PRIVATE TRAFFICSIM_SOURCE_DIR="${PROJECT_SOURCE_DIR}")
    add_test(NAME network-lifecycle-ui COMMAND trafficsim-lifecycle-ui-tests)
    set_tests_properties(network-lifecycle-ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 60)
    add_dependencies(check trafficsim-lifecycle-ui-tests)
    add_executable(trafficsim-demand-ui-tests ../../tests/demand_ui_tests.cpp)
    target_link_libraries(trafficsim-demand-ui-tests PRIVATE trafficsim_shell Qt6::Test trafficsim_options)
    add_test(NAME demand-ui COMMAND trafficsim-demand-ui-tests "${PROJECT_SOURCE_DIR}/data")
    set_tests_properties(demand-ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 60)
    add_dependencies(check trafficsim-demand-ui-tests)
    add_executable(trafficsim-authoring-ui-tests ../../tests/authoring_ui_tests.cpp)
    target_link_libraries(trafficsim-authoring-ui-tests PRIVATE trafficsim_shell Qt6::Test trafficsim_options)
    add_test(NAME authoring-ui COMMAND trafficsim-authoring-ui-tests "${PROJECT_SOURCE_DIR}/data")
    set_tests_properties(authoring-ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 45)
    add_dependencies(check trafficsim-authoring-ui-tests)
    add_executable(trafficsim-scenario-run-tests ../../tests/scenario_run_ui_tests.cpp)
    target_link_libraries(trafficsim-scenario-run-tests PRIVATE trafficsim_shell Qt6::Test trafficsim_options)
    add_test(NAME scenario-run-ui COMMAND trafficsim-scenario-run-tests "${PROJECT_SOURCE_DIR}/data")
    set_tests_properties(scenario-run-ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 90)
    add_executable(trafficsim-editor-tests ../../tests/editor_ui_tests.cpp)
    target_link_libraries(trafficsim-editor-tests PRIVATE trafficsim_shell Qt6::Test trafficsim_options)
    add_test(NAME editor-ui COMMAND trafficsim-editor-tests "${PROJECT_SOURCE_DIR}/data")
    set_tests_properties(editor-ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 45)
    add_executable(trafficsim-connector-tests ../../tests/connector_ui_tests.cpp)
    target_link_libraries(trafficsim-connector-tests PRIVATE trafficsim_shell Qt6::Test trafficsim_options)
    add_test(NAME connector-ui COMMAND trafficsim-connector-tests "${PROJECT_SOURCE_DIR}/data")
    set_tests_properties(connector-ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 45)
    add_executable(trafficsim-table-tests ../../tests/table_ui_tests.cpp)
    target_link_libraries(trafficsim-table-tests PRIVATE trafficsim_shell Qt6::Test trafficsim_options)
    add_test(NAME tables-ui COMMAND trafficsim-table-tests "${PROJECT_SOURCE_DIR}/data")
    set_tests_properties(tables-ui PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 45)
    add_executable(trafficsim-m1-tests ../../tests/m1_ui_tests.cpp)
    target_link_libraries(trafficsim-m1-tests PRIVATE trafficsim_shell Qt6::Test trafficsim_options)
    add_test(NAME m1-workflow COMMAND trafficsim-m1-tests "${PROJECT_SOURCE_DIR}/data")
    set_tests_properties(m1-workflow PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 60)
    add_dependencies(check trafficsim-m1-tests)
    add_executable(trafficsim-attachment-tests ../../tests/attachment_ui_tests.cpp)
    target_link_libraries(trafficsim-attachment-tests PRIVATE trafficsim_shell Qt6::Test trafficsim_options)
    add_test(NAME editor-attachments COMMAND trafficsim-attachment-tests "${PROJECT_SOURCE_DIR}/data")
    set_tests_properties(editor-attachments PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 60)
    add_dependencies(check trafficsim-attachment-tests)
    add_executable(trafficsim-gesture-tests ../../tests/gesture_ui_tests.cpp)
    target_link_libraries(trafficsim-gesture-tests PRIVATE trafficsim_shell Qt6::Test trafficsim_options)
    add_test(NAME editor-gestures COMMAND trafficsim-gesture-tests "${PROJECT_SOURCE_DIR}/data")
    set_tests_properties(editor-gestures PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 60)
    add_dependencies(check trafficsim-gesture-tests)
    add_dependencies(check trafficsim-scenario-run-tests trafficsim-editor-tests trafficsim-connector-tests trafficsim-table-tests)
endif()
